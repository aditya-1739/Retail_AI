# Database URL connection string (defaults to local SQLite db)
DATABASE_URL=sqlite:///./smart_retail.db

# JWT signing secret key (change this in production)
SECRET_KEY=supersecretkeychangeinprod123!

# CORS Allowed Origins (comma-separated if multiple)
ALLOWED_ORIGINS=http://localhost:5173
